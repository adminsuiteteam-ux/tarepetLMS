web: python manage.py migrate --no-input || true; python manage.py seed_lms_data || true; daphne -b 0.0.0.0 -p $PORT config.asgi:application || gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
